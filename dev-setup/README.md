# Dev setup (optional)

Scripts to smoke-test that **`track-b`** data loads. **Not part of Product 1 grading** — reviewers use **`Mystri-Applicant-Assessments/track-b/HANDOVER.md`**.

If you use Cursor Cloud Agent with a custom environment path, point install at **`dev-setup/cloud-agent-install.sh`**.
