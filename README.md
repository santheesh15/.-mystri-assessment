# Mystri assessment — Track B submission

**Reviewers:** This repository’s **submission is not on `main`.**

| | |
| --- | --- |
| **Repository** | `https://github.com/santheesh15/.-mystri-assessment` |
| **Submission branch** | **`cursor/track-b-submission-edb7`** |

### Switch branch on GitHub (`main` → submission)

1. Open the repo (it may default to **`main`**).
2. Click the **branch** dropdown (next to the file list).
3. Select **`cursor/track-b-submission-edb7`**.
4. Confirm the URL contains `/tree/cursor/track-b-submission-edb7/`.

### After clone (if you are still on `main`)

```powershell
git fetch origin
git checkout cursor/track-b-submission-edb7
```

Then open **`Mystri-Applicant-Assessments/README.md`** and **`Mystri-Applicant-Assessments/track-b/HANDOVER.md`** for verify steps (`python product1\verify_product1.py` → **`PRODUCT 1 PASS`**).
